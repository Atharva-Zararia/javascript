//whenever we to do some actins after some specific time we use timing events
//1) set timeout executes a function, after waiting a specified number of milliseconds(executes only once)

        // setTimeout(function,milliseconds);
    // to stop execution of the function specified in setTimeOut()
        // clearTimeout(timeoutVariable)



//2) setInterval - same as setTimeout(),but repeats the execution of the function continously
        //setInterval(function,milliseconds);
    //To stop the executions of the function specified in the setInterval() method.
        //clearInterval(timerVariable)